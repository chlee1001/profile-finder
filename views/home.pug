extends layouts/main
include mixins/recruitBlock

block content
    .recruits
        each item in recruits
            +recruitBlock({
                id:item.id,
                title:item.title,
                views:item.views,
            })